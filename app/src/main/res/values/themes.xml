<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SudokuLanguagePracticeApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SudokuLanguagePracticeApp" parent="Base.Theme.SudokuLanguagePracticeApp" />

    <!-- Cite: https://stackoverflow.com/a/25365193 -->
    <style name="Theme.SudokuLanguagePracticeApp.FullScreen" parent="Base.Theme.SudokuLanguagePracticeApp">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>